# function_name <- function(arg1, arg2=T, ...){
# <Body of the Function>
# return <Return Value>
# }

# filter_table2 <- subset(demo_table2, price >= 1000 & drive == "4wd" & "clean" 
# %in% title_status)
# filter by price and drive


# nrow(): counts the number of rows

#hjust tells ggplot that our rotated labels should be aligned horizontally to 
# our tick marks